<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_learn"
    android:gravity="center"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Back To School"
        android:textSize="60dp"
        android:gravity="center"
        android:fontFamily="@font/boogaloo_regular"
        android:textColor="#f8f8f8"/>
    <TextView
        android:id="@+id/textView2"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:text="The Easy Way To Teach Your Child"
        android:textSize="21dp"
        android:gravity="center"
        android:fontFamily="@font/boogaloo_regular"
        android:textColor="#f8f8f8"/>

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:layout_marginTop="10dp"
        android:padding="30dp"
        android:src="@drawable/ic_start" />

    <Button
        android:id="@+id/btnStart"
        android:onClick="onStartClick"
        android:clickable="true"
        android:layout_width="200dp"
        android:layout_height="45dp"
        android:background="@drawable/bg_button_cevap"
        android:text="Start Learnıng"
        android:textSize="22dp"
        android:textColor="#f8f8f8"
        android:layout_marginTop="25dp"
        android:fontFamily="@font/boogaloo_regular"
        android:scaleType="centerInside"></Button>




















</LinearLayout>